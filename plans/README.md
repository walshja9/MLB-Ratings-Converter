# Implementation Plans

Generated by the `/improve` skill on 2026-06-13, against commit `bcfbd39`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update your
row when done.

**Repo-specific note:** this is a PUBLIC GitHub repo
(`github.com/walshja9/MLB-Ratings-Converter`). Per the owner's rules, do NOT
`git commit` or `git push` as part of executing any plan — make the file changes,
verify them, and leave committing to the owner. The test runner is the shared
sibling-project venv:
`"C:/Users/Alex/Claude Context/Robert Stock - Pitching Grade Model/venv/Scripts/python.exe"`.

## Baseline at planning time

`pytest` reports **24 failed, 143 passed** in the working tree (167 tests).
All 24 failures are `*_preservation` / `*_unchanged` tests that hardcode
pre-6/4-recalibration coefficients. The committed HEAD is *also* red (29 of 157
tracked tests) — the suite has been out of sync independent of the uncommitted
refit. Plan 001 is what makes the suite green and trustworthy; most other plans
assume it has landed.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Reconcile 6/4 refit: fix 24 stale preservation tests + update docs → green suite | P1 | M | — | DONE |
| 002 | Fix reliever stamina: openers/swingmen wrongly get SP stamina | P1 | S | 001 | DONE |
| 003 | Add pytest config, conftest, one-command runner + CI | P1 | S | 001 | DONE |
| 004 | Bind Flask to localhost by default; opt-in LAN exposure via env | P2 | S | — | DONE |
| 005 | Stop tracking bloated/personal data artifacts | P2 | S | — | DONE (show_truth.json untracked per owner: keep it private; all 3 files still on disk) |
| 006 | Archive ~21 dead one-off scripts | P2 | S | — | DONE (24 scripts moved; recalibrate.py moved per owner decision; scrape_teams.py kept in root) |
| 007 | Harden stat parsing: NaN-safe ints, KeyError-safe sorts, no silent swallows | P2 | M | 001, 003 | DONE |
| 008 | Pin dependencies + add lockfile and dev requirements | P3 | S | — | DONE (requirements.lock created with == pins; requirements.txt upper bounds added) |
| 009 | De-duplicate PITCH_NAMES (fix latent SP→Splitter bug) + consolidate safe_float | P3 | S | 001, 003 | DONE |
| 010 | Make the ValuCast input path configurable + warn when missing | P3 | S | — | DONE (env-var override + stderr warning added; HANDOFF.md documented) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

## Dependency notes

- **002, 003, 007, 009 depend on 001** only in the sense that they should land on
  a *green* suite. 001 makes "all tests pass" the meaningful success signal those
  plans verify against. The code changes themselves don't overlap with 001.
- **003 depends on 001** more strongly: a CI gate that runs on a red suite is
  useless. Land green first, then wire CI.
- 004, 005, 006, 008, 010 are independent and can be done in any order.

## Findings considered and rejected

- **"`pct()` percentile is off-by-one and biases every prospect grade down"** —
  rejected. `expand_prospects.py:51-60` is a clean linear rank for distinct values
  (the existing `test_pct_monotonic_and_inversion` encodes this as correct); ties
  land at the high end of their band, not a systematic *downward* bias. Methodology
  choice, not a bug.
- **Splitting `generate_card.py` (2,844-line god file) per the
  `docs/superpowers/plans/2026-05-03-modular-refactor.md` plan** — real debt, but
  L-effort and HIGH-risk (touches `app.py`, all test files, `recalibrate_all.py`),
  with no functional payoff. Not worth doing as part of this batch; revisit only if
  the file becomes an active bottleneck. Plan 009 takes the cheap first slice
  (de-duplicating the constants) without the full refactor.
